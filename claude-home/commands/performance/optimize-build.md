---
description: "Optimize build processes and speed"
---

ビルドプロセスとビルド速度を最適化する。

手順の詳細は `performance` Skill の `references/build-optimization.md` を参照。

1. `performance` Skill の `references/build-optimization.md` を Read して手順を確認
2. 手順に従いビルドを最適化
3. 最適化前後のビルド時間を比較して報告

See also: `/performance:optimize-bundle-size`, `/performance:performance-audit`
