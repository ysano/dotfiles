---
description: "Implement GraphQL API endpoints"
---

GraphQL API エンドポイントを実装する。

手順の詳細は `setup` Skill の `references/graphql-api.md` を参照。

1. `setup` Skill の `references/graphql-api.md` を Read して手順を確認
2. GraphQL サーバーをセットアップし、スキーマを定義
3. リゾルバ・認証・サブスクリプションを実装
4. パフォーマンス最適化・テストを実施
5. 結果を報告

See also: `/setup:design-rest-api`, `/setup:setup-rate-limiting`, `/performance:performance-audit`
