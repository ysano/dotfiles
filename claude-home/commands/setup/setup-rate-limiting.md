---
description: "Implement API rate limiting"
---

API レート制限を実装する。

手順の詳細は `setup` Skill の `references/rate-limiting.md` を参照。

1. `setup` Skill の `references/rate-limiting.md` を Read して手順を確認
2. レート制限戦略を策定し、アルゴリズムを選定
3. 実装レイヤーとストレージバックエンドを選定
4. モニタリング・アラートを設定
5. 結果を報告

See also: `/setup:design-rest-api`, `/setup:implement-graphql-api`, `/security:security-hardening`
