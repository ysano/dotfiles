---
description: "Setup automated release workflows"
---

自動リリースワークフローを構築する。

手順の詳細は `deploy` Skill の `references/setup-automated-releases.md` を参照。

1. `deploy` Skill の `references/setup-automated-releases.md` を Read して手順を確認
2. 手順に従い Conventional Commits ベースの自動リリースを設定
3. ワークフロー実行を検証し結果を報告

See also: `/deploy:prepare-release`, `/deploy:add-changelog`
