---
description: "Configure Kubernetes deployment manifests"
---

Kubernetes デプロイ設定を構築する。

手順の詳細は `deploy` Skill の `references/setup-kubernetes-deployment.md` を参照。

1. `deploy` Skill の `references/setup-kubernetes-deployment.md` を Read して手順を確認
2. 手順に従い Kubernetes マニフェストを作成
3. デプロイ結果と Pod 状態を報告

See also: `/deploy:containerize-application`, `/deploy:rollback-deploy`
