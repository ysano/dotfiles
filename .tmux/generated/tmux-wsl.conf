# Generated TMux Configuration for WSL
# Auto-generated from tmux-unified.yaml

# Basic configuration
set -g default-command zsh
unbind C-b
set -g prefix C-z
bind C-z send-prefix
bind z send-prefix

set -g base-index 1
set -g pane-base-index 1

set -g set-titles on
set-option -g default-terminal "xterm-256color"
set -as terminal-overrides ",xterm-termite:Tc"
set -as terminal-overrides ",*:U8=0"

set -g display-time 2000
set-option -g automatic-rename on
set-option -g automatic-rename-format "#{=|-22|..;b:pane_current_path}"

# Mouse configuration
set -g mouse on

# WSL-specific configuration
set-environment -g WSL_INTEGRATION "enabled"
set-environment -g WINDOWS_HOST_PATH "/mnt/c"

# Basic clipboard integration
if-shell "grep -qi microsoft /proc/version 2>/dev/null && command -v clip.exe" "bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "clip.exe""

# Configuration generated successfully
# Generated at: 2025年  8月  5日 火曜日 06:38:37 JST
# Platform: wsl
