# Modern tmux Appearance Configuration
# リファクタリング版: モダンなテーマとクリーンな設定

# Modern tmux Appearance Configuration
# リファクタリング版: 直接値指定によるクリーンな設定

# === Modern Color Theme (tmux 2.9+) ===
# ステータスバー
set -g status-style                  "fg=colour250,bg=colour235"

# ウィンドウステータス
set -wg window-status-style          "fg=colour250,bg=colour235,none"
set -wg window-status-current-style  "fg=colour117,bg=colour238,bold"

# ペイン境界
set -g pane-border-style             "fg=colour238"
set -g pane-active-border-style      "fg=colour117"

# メッセージ
set -g message-style                 "fg=colour117,bg=colour238,bold"
set -g message-command-style         "fg=colour117,bg=colour238,bold"

# ディスプレイ
set -g display-panes-active-colour   "colour117"
set -g display-panes-colour          "colour250"
set -wg clock-mode-colour            "colour117"

# アラートとアクティビティ
set -g window-status-bell-style      "fg=colour196,bg=colour235,bold"
set -g window-status-activity-style  "fg=colour10,bg=colour235,bold"

# === 視覚的改善 (tmux 3.0+) ===
# ペイン境界を目立たせる（対応バージョンのみ）
if-shell 'tmux -V | cut -d" " -f2 | awk "BEGIN{FS=\".\"} {exit !(\$1 > 2 || (\$1 == 3 && \$2 >= 0))}"' \
  "set -g pane-border-lines heavy"

# === 高コントラストモード（環境変数対応） ===
if-shell '[ "${TMUX_HIGH_CONTRAST:-false}" = "true" ]' \
  "set -g window-status-current-style 'fg=black,bg=colour117,bold'"