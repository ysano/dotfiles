#!/bin/bash
# Claude Voice - Notification + Voice Demo
# 通知音の後に音声を再生するデモンストレーション

CLAUDE_VOICE_HOME="${CLAUDE_VOICE_HOME:-$HOME/.tmux/claude}"
source "$CLAUDE_VOICE_HOME/core/spatial_audio.sh"

# macOSシステム通知音の定義
SOUND_COMPLETE="/System/Library/Sounds/Glass.aiff"     # ✅ 完了: Glass音
SOUND_WAITING="/System/Library/Sounds/Tink.aiff"       # ⌛ 待機: Tink音
SOUND_BUSY="/System/Library/Sounds/Basso.aiff"         # ⚡ 忙しい: Basso音
SOUND_ERROR="/System/Library/Sounds/Hero.aiff"         # ❌ エラー: Hero音
SOUND_INFO="/System/Library/Sounds/Pop.aiff"           # ℹ️ 情報: Pop音

# 通知音を再生
play_notification_sound() {
    local sound_file="$1"
    local description="$2"
    
    echo "🔔 Playing: $description"
    
    # macOSのafplayコマンドでシステム音を再生
    if [[ "$OSTYPE" == "darwin"* ]]; then
        if [[ -f "$sound_file" ]]; then
            afplay "$sound_file" 2>/dev/null
        else
            echo "   ⚠️ Sound file not found: $sound_file"
        fi
    fi
}

# 通知音 + 音声のデモ
demo_notification_with_voice() {
    local status="$1"
    local message="$2"
    local voice="${3:-Kyoko}"
    
    case "$status" in
        "busy")
            echo "⚡ 忙しい状態の通知 (Basso)"
            play_notification_sound "$SOUND_BUSY" "Basso (Busy)"
            sleep 0.5
            say -v "$voice" -r 200 "$message" 2>/dev/null
            ;;
        "waiting")
            echo "⌛ 待機状態の通知 (Tink)"
            play_notification_sound "$SOUND_WAITING" "Tink (Waiting)"
            sleep 0.5
            say -v "$voice" -r 200 "$message" 2>/dev/null
            ;;
        "complete")
            echo "✅ 完了状態の通知 (Glass)"
            play_notification_sound "$SOUND_COMPLETE" "Glass (Complete)"
            sleep 0.5
            say -v "$voice" -r 200 "$message" 2>/dev/null
            ;;
        "error")
            echo "❌ エラー状態の通知 (Hero)"
            play_notification_sound "$SOUND_ERROR" "Hero (Error)"
            sleep 0.5
            say -v "$voice" -r 200 "$message" 2>/dev/null
            ;;
        "info")
            echo "ℹ️ 情報通知 (Pop)"
            play_notification_sound "$SOUND_INFO" "Pop (Info)"
            sleep 0.5
            say -v "$voice" -r 200 "$message" 2>/dev/null
            ;;
    esac
}

# 空間音響付き通知デモ
demo_spatial_notification() {
    echo "🎭 Spatial Notification Demo"
    echo "============================"
    echo ""
    
    # Claude Codeウィンドウを検出
    local claude_windows=()
    while IFS= read -r window_id; do
        [[ -n "$window_id" ]] && claude_windows+=("$window_id")
    done <<< "$(detect_claude_windows)"
    
    local window_count=${#claude_windows[@]}
    
    if [[ $window_count -eq 0 ]]; then
        echo "❌ No Claude Code windows detected"
        echo "📝 Using simulated windows for demo..."
        claude_windows=(2 3 4 5)
        window_count=4
    fi
    
    echo "📍 Testing with $window_count window(s): ${claude_windows[*]}"
    echo ""
    
    # 各ウィンドウに対して通知音と音声を再生
    local index=0
    for window_id in "${claude_windows[@]}"; do
        # パンニング係数を計算
        local panning=$(calculate_db_panning "$window_count" "$index")
        IFS='|' read -r left_gain right_gain <<< "$panning"
        
        # 音声を自動割り当て
        local voice=$(assign_voice_to_window "$index")
        
        # ステータスをランダムに選択（デモ用）
        local statuses=("complete" "waiting" "busy")
        local status="${statuses[$((RANDOM % 3))]}"
        
        # 位置の説明
        local position=""
        if [[ $window_count -eq 1 ]]; then
            position="中央"
        elif [[ $index -eq 0 ]]; then
            position="左端"
        elif [[ $index -eq $((window_count-1)) ]]; then
            position="右端"
        else
            position="中央"
        fi
        
        echo "Window $window_id ($position): $voice"
        
        # ステータスに応じた通知音を再生
        case "$status" in
            "busy")
                echo "  ⚡ 処理中..."
                afplay "$SOUND_BUSY" 2>/dev/null
                ;;
            "waiting")
                echo "  ⌛ 待機中..."
                afplay "$SOUND_WAITING" 2>/dev/null
                ;;
            "complete")
                echo "  ✅ 完了!"
                afplay "$SOUND_COMPLETE" 2>/dev/null
                ;;
        esac
        
        sleep 0.3
        
        # 音声メッセージを再生
        local message="ウィンドウ${window_id}の処理が完了しました"
        
        # 空間配置された音声を再生
        local temp_audio="/tmp/claude_notify_${window_id}.aiff"
        say -v "$voice" -r 200 -o "$temp_audio" "$message" 2>/dev/null
        
        if command -v ffplay >/dev/null 2>&1; then
            # パンニング付きで再生
            ffplay -nodisp -autoexit -loglevel quiet \
                -af "pan=stereo|c0=${left_gain}*c0|c1=${right_gain}*c0" \
                "$temp_audio" 2>/dev/null &
        else
            # フォールバック
            afplay "$temp_audio" 2>/dev/null &
        fi
        
        rm -f "$temp_audio"
        
        sleep 1.5
        ((index++))
    done
    
    wait
    echo ""
    echo "✅ Demo completed"
}

# シンプルな連続デモ
simple_demo() {
    echo "🎵 Simple Notification + Voice Demo"
    echo "==================================="
    echo ""
    
    echo "1. 完了通知のデモ (Glass)"
    echo "-------------------------"
    afplay "$SOUND_COMPLETE" 2>/dev/null
    sleep 0.5
    say -v "Kyoko" -r 200 "処理が完了しました" 2>/dev/null
    sleep 1.5
    
    echo ""
    echo "2. 待機通知のデモ (Tink)"
    echo "------------------------"
    afplay "$SOUND_WAITING" 2>/dev/null
    sleep 0.5
    say -v "Otoya" -r 200 "処理を待機しています" 2>/dev/null
    sleep 1.5
    
    echo ""
    echo "3. エラー通知のデモ (Hero)"
    echo "-------------------------"
    afplay "$SOUND_ERROR" 2>/dev/null
    sleep 0.5
    say -v "Kyoko (Enhanced)" -r 200 "エラーが発生しました" 2>/dev/null
    sleep 1.5
    
    echo ""
    echo "4. 忙しい通知のデモ (Basso)"
    echo "---------------------------"
    afplay "$SOUND_BUSY" 2>/dev/null
    sleep 0.5
    say -v "Otoya" -r 200 "処理中です" 2>/dev/null
    sleep 1.5
    
    echo ""
    echo "5. 情報通知のデモ (Pop)"
    echo "----------------------"
    afplay "$SOUND_INFO" 2>/dev/null
    sleep 0.5
    say -v "Kyoko" -r 200 "新しい情報があります" 2>/dev/null
    
    echo ""
    echo "✅ Demo completed"
}

# メインメニュー
show_menu() {
    echo ""
    echo "🔊 Notification + Voice Demo"
    echo ""
    echo "Select demo type:"
    echo "  1) Simple demo (sequential)"
    echo "  2) Spatial demo (with panning)"
    echo "  3) Individual status test"
    echo "  4) Exit"
    echo ""
    echo -n "Option: "
}

# メイン処理
main() {
    if [[ $# -gt 0 ]]; then
        case "$1" in
            "simple")
                simple_demo
                ;;
            "spatial")
                demo_spatial_notification
                ;;
            "complete")
                demo_notification_with_voice "complete" "処理が完了しました"
                ;;
            "waiting")
                demo_notification_with_voice "waiting" "待機中です"
                ;;
            "busy")
                demo_notification_with_voice "busy" "処理中です"
                ;;
            *)
                echo "Usage: $(basename "$0") [simple|spatial|complete|waiting|busy]"
                exit 1
                ;;
        esac
    else
        while true; do
            show_menu
            read -r option
            
            case "$option" in
                1)
                    echo ""
                    simple_demo
                    ;;
                2)
                    echo ""
                    demo_spatial_notification
                    ;;
                3)
                    echo ""
                    echo "Select status:"
                    echo "  1) Complete (✅)"
                    echo "  2) Waiting (⌛)"
                    echo "  3) Busy (⚡)"
                    echo -n "Status: "
                    read -r status_choice
                    
                    case "$status_choice" in
                        1) demo_notification_with_voice "complete" "処理が完了しました" ;;
                        2) demo_notification_with_voice "waiting" "待機中です" ;;
                        3) demo_notification_with_voice "busy" "処理中です" ;;
                        *) echo "Invalid choice" ;;
                    esac
                    ;;
                4)
                    echo "Goodbye!"
                    exit 0
                    ;;
                *)
                    echo "Invalid option"
                    ;;
            esac
        done
    fi
}

# スクリプト実行
if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
    main "$@"
fi